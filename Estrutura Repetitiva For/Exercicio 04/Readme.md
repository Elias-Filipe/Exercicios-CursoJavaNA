# Exercício 04 — Divisão entre Pares de Números

## 🇧🇷 Descrição:
Este exercício tem como objetivo praticar o uso de estruturas de repetição, leitura de números e tratamento de erros aritméticos.  
O programa lê um valor inteiro **N**, que representa a quantidade de pares de números a serem processados.  
Para cada par, o programa realiza a **divisão do primeiro número pelo segundo** e exibe o resultado.  
Se o segundo número (denominador) for igual a zero, o programa deve exibir a mensagem **"divisao impossivel"**.

## 🇺🇸 Description:
This exercise aims to practice the use of loops, number reading, and arithmetic error handling.  
The program reads an integer **N**, which represents the number of pairs of numbers to be processed.  
For each pair, the program performs the **division of the first number by the second** and displays the result.  
If the second number (denominator) is equal to zero, the program must display the message **"divisao impossivel"**.
